[build-system]
requires = [
    "scikit-build-core",
    "numpy>=2.0.0",
]
build-backend = "scikit_build_core.build"

[project]
name = "sdf-xarray"
version = "0.0.1"
requires-python = ">=3.9"
dependencies = [
  "numpy>=2.0.0",
]
